<?xml version="1.0" encoding="utf-8"?>
<!--
    מסך דוח התקדמות.

    מבנה:
    - Header עם כותרת "דוח התקדמות" (אנחנו לא משתמשים יותר ב-Toolbar
      עם setSupportActionBar כי זה משבר את ה-drawer toggle של GoalsActivity)
    - שורת chips להחלפת טווח תאריך (השבוע / החודש / 3 חודשים)
    - RecyclerView עם כרטיסיות התקדמות
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:attr/colorBackground"
    tools:context=".ui.ProgressFragment">

    <!-- Header אזור -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="16dp"
        android:paddingBottom="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:letterSpacing="-0.02"
            android:text="@string/progress_title"
            android:textColor="?attr/colorOnSurface"
            android:textSize="28sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="עקבי אחרי ההצלחות שלך"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="13sp" />

    </LinearLayout>

    <!-- Chips של טווח תאריך - גלילה אופקית אם צריך -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chipGroupDateRange"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="8dp"
            android:paddingBottom="12dp"
            app:singleSelection="true"
            app:selectionRequired="true">

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_week"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:text="@string/progress_range_week" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_month"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:text="@string/progress_range_month" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_quarter"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:text="@string/progress_range_quarter" />

        </com.google.android.material.chip.ChipGroup>
    </HorizontalScrollView>

    <!-- רשימת כרטיסיות התקדמות -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvProgress"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingTop="4dp"
        android:paddingBottom="80dp"
        android:layoutAnimation="@anim/layout_animation_fall_down"
        tools:listitem="@layout/item_goal_progress" />

</LinearLayout>
